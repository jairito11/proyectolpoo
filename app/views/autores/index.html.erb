<p id="notice"><%= notice %></p>


<div class="container">
<div class="col-sm-10 col-sm offset-1">
  <h1 class="text-center">Autores</h1>
<table class="bg-info rounded">
  <thead>
    <tr>
      <th class="form-group">Nombre</th>
      <th class="form-group">Paterno</th>
      <th class="form-group">Materno</th>
      <th class="form-group">Nacionalidad</th>
      <th class="form-group">Lengua</th>
      <th class="form-group">Religion</th>
      <th class="form-group">Distinciones</th>
      <th class="form-group">Fecha de nacimiento</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @autores.each do |autor| %>
      <tr>
        <td><%= autor.nombre %></td>
        <td><%= autor.paterno %></td>
        <td><%= autor.materno %></td>
        <td><%= autor.nacionalidad %></td>
        <td><%= autor.lengua %></td>
        <td><%= autor.religion %></td>
        <td><%= autor.distinciones %></td>
        <td><%= autor.fechaNacimiento %></td>
        <td><%= link_to 'Mostrar', autor , :class => "btn bg-success text-white rounded" %></td>
        <td><%= link_to 'Editar', edit_autor_path(autor) , :class => "btn bg-success text-white rounded" %></td>
        <td><%= link_to 'Eliminar', autor, method: :delete, data: { confirm: 'Estás seguro?' } , :class => "btn bg-success text-white rounded" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
</div>

<br>

<%= link_to 'Nuevo Autor', new_autor_path %>

